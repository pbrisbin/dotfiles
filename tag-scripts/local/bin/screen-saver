#!/bin/sh
case "$1" in
  on)
    xset +dpms
    xautolock -time 5 -locker slock -secure &
    ;;
  off)
    xset -dpms
    pkill xautolock
    ;;
  *)
    echo "usage: screen-saver [on|off]" >&2
    exit 64
    ;;
esac
