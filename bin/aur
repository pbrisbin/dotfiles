#!/usr/bin/env sh
#
# pbrisbin 2013 - stupid simple, feature-less aur installer.
#
###
to_package_url() {
  sed 's|^\(..\).*|https://aur.archlinux.org/packages/\1/&/&.tar.gz|'
}

curl -# $(printf "$1" | to_package_url) | tar fxz - \
  && cd "./$1" && makepkg -i -r -s \
  && cd ..     && rm -r "$1"
